'use client';import{motion,useInView,useMotionValue,useSpring}from'framer-motion';import{useEffect,useRef}from'react';export default function CountUp({to,suffix=''}:{to:number,suffix?:string}){const ref=useRef(null);const mv=useMotionValue(0);const spring=useSpring(mv,{duration:3000});const isInView=useInView(ref,{once:true});useEffect(()=>{if(isInView)mv.set(to)},[isInView,to,mv]);useEffect(()=>spring.on('change',v=>{if(ref.current)(ref.current as any).textContent=Math.floor(v)+suffix}),[spring,suffix]);return<span ref={ref}>0</span>}